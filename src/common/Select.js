import React from "react";

const Select = () => {
  return (
    <div>
      <label>Reason</label>
      <select>
        <option></option>
      </select>
    </div>
  );
};

export default Select;
